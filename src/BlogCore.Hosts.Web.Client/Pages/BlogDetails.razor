@page "/blog/{BlogId:guid}"
@inject GuardService GuardService

<div class="container">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    Create new Blog
                </div>
                <div class="card-body">
                    <EditForm Model="@Model" OnValidSubmit="@SubmitValidForm">
                        <FluentValidationValidator />
                        <ValidationSummary />
                        <div class="form-group">
                            <label for="title" class="col-sm-2 control-label">Title</label>
                            <div class="col-sm-10">
                                <InputText @bind-Value="@Model.Title" class="form-control" id="title" placeholder="title"></InputText>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="description" class="col-sm-2 control-label">Description</label>
                            <div class="col-sm-10">
                                <InputText @bind-Value="@Model.Description" class="form-control" id="description" placeholder="description"></InputText>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="posts-per-page" class="col-sm-2 control-label">Posts per Page</label>
                            <div class="col-sm-10">
                                <InputNumber @bind-Value="@Model.PostsPerPage" class="form-control" id="posts-per-page" placeholder="posts per page"></InputNumber>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="days-to-comment" class="col-sm-2 control-label">Days to Comment</label>
                            <div class="col-sm-10">
                                <InputNumber @bind-Value="@Model.DaysToComment" class="form-control" id="days-to-comment" placeholder="days to comment"></InputNumber>
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="moderate-comments" class="col-sm-2 control-label">Moderate Comments</label>
                            <div class="col-sm-10">
                                <InputCheckbox @bind-Value="@Model.ModerateComments" class="form-control" id="moderate-comments" placeholder="moderate-comments"></InputCheckbox>
                            </div>
                        </div>
                        <div class="form-group">
                            <div class="col-sm-offset-2 col-sm-10">
                                <button type="submit" class="btn btn-primary">Save</button>
                            </div>
                        </div>
                    </EditForm>
                </div>
            </div>
        </div>
    </div>
</div>


@code {
    [Parameter]
    Guid BlogId { get; set; }

    CreateBlogRequest Model { get; set; } = new CreateBlogRequest();

    protected override async Task OnInitAsync()
    {
        await GuardService.Check();
    }

    void SubmitValidForm()
    {
        Console.WriteLine("OnValidSubmit");
    }
}
